{{#each this}}
  <div class="country-card">
    <img src="{{flags.svg}}" alt="{{name.official}}"" width="300"/>
    <h2>{{name.official}}</h2>
    <p class="country-capital">Capital: {{capital}}</p>
    <p class="country-population">Population: {{population}}</p>
    <p class="country-languages">Languages:
        {{#each languages}}
        {{this}}
        {{/each}}
    </p>
  </div>
{{/each}}